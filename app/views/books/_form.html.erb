<%= form_for([@author, @author.books.build]) do |f| %>
    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </p>
    <p>
      <%= f.label :year %><br>
      <%= f.text_field :year %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>